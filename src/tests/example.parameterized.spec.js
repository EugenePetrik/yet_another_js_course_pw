// @ts-check
import { test } from '@playwright/test';

test.describe('Simon Smith app basic tests (parameterized)', { tag: '@ui' }, () => {
  test('should perform search', async () => {});

  test('should open user profile', async () => {});

  test('should open playwright_tests_with_po repository', async () => {});
});
